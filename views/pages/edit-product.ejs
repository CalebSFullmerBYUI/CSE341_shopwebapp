<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <% include ../includes/header.ejs %>
    </head>

    <body>
        <% include ../includes/banner.ejs%>
        <div style="display: flex; flex-direction: column; align-items: center; padding: 10px;">
            <form action="<% if (product.productID != -1) { %>/edit-product/<%= product.productID %><% } else { %>/add-product<% } %>" method="POST" style="display: flex; flex-direction: column; width: 50%;">
                <div style="margin-bottom: 10px;">
                    <label>Product Name</label>
                    <input type="text" name="productName" style="width: 100%" value="<%= product.name %>">
                </div>
                <div style="margin-bottom: 10px;">
                    <label>Price</label>
                    <input type="number" name="productPrice" min="0" step="0.01" style="width: 100%" value="<%= product.price %>">
                </div>
                <div style="margin-bottom: 10px;">
                    <label>Description</label>
                    <textarea type="textarea" name="productDescription" rows="5"
                        style="width: 100%; resize: none; overflow-y: scroll;"><%= product.description %></textarea>
                </div>
                <div style="margin-bottom: 10px;">
                    <label>Picture URL</label>
                    <input type="text" name="pictureURL" style="width: 100%" value="<%= product.pictureURL %>">
                </div>
                <input type="submit" value="submit">
            </form>
        </div>
    </body>
</html>